#ifndef PROJECT_CALENDER_H
#define PROJECT_CALENDER_H

int dayNumber(int day, int month, int year);
char* getMonthName(int monthNumber);
int numberOfDays(int monthNumber, int year);
void printCalendar(int year);

#endif